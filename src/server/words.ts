const CHARS = `abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ`
const CHARS_ARR = Array.from(CHARS)

export const seperate = (str: string): string[] => {
	const arr = Array.from(str)
	const words = []
	let currWord = ""
	arr.forEach((char) => {})

	return
}
